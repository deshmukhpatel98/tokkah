{
  "name": "tokkah",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "1:1 video calls with bit-exact lossless audio, visually lossless video, and sub-10ms packet repair \u2014 on a single Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev --port 8794",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy -c wrangler.prod.jsonc",
    "log": "wrangler tail",
    "test": "node turntaking.test.mjs && node public/core/pcmsw.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260812.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.121.0"
  }
}
