{
  "name": "tokkah",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "1:1 video calls with bit-exact lossless audio, visually lossless video, and sub-10ms packet repair — on a single Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev --port 8794",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy -c wrangler.prod.jsonc",
    "log": "wrangler tail",
    "test": "node turntaking.test.mjs && node public/core/pcmsw.test.mjs && node diagnose.test.mjs && node contacts.test.mjs && node routing.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260829.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  }
}
